FROM python:3.11-slim

WORKDIR /app

COPY --from=ghcr.io/astral-sh/uv:latest /uv /bin/uv

# Workspace config
COPY pyproject.toml uv.lock ./
COPY services/common ./services/common
COPY services/document_service ./services/document_service

RUN sed -i 's|\"./services/model_service\",||g; s|\"./services/search_service\",||g; s|\"./services/gateway_service\",||g; s|\"./services/user_service\",||g' pyproject.toml

# 4. Test config
COPY .env.test .env

# 5. Install
RUN uv sync --package document_service --package semantic_search_app --group dev

CMD ["uv", "run", "pytest", "services/document_service/tests", "-v"]