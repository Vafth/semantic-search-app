apiVersion: v1
kind: Service
metadata:
  name: search-service
spec:
  selector:
    app: search-service
  ports:
    - port: {{ .Values.searchService.port }}
      targetPort: {{ .Values.searchService.port }}
  type: ClusterIP 